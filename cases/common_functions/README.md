# Common Case Functions

Shared helpers for case scripts live here when they are specific to case
analysis rather than general package functionality.

Keep reusable library code under the package modules. Use this directory for
small report-building utilities, summary loaders, and plotting helpers shared
by multiple case reports.

