# Convolutional Cases

This directory contains CIFAR/image and convolutional DRN case reports.

Track:

- report READMEs
- small summary CSV/JSON files
- analysis scripts needed to reproduce the summarized result

Do not track:

- `runs/` directories
- checkpoints
- TensorBoard events
- raw generated outputs

