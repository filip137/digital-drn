# Transformer Cases

This directory is reserved for MQAR, GPT-style, and attention-related DRN case
reports.

Use `simulation_results/transformer_*` or an ignored local `runs/` directory for
raw training output. Commit only curated summaries and scripts that are useful
for reproducing or explaining the result.

